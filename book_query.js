db.books.find({});

// db.books.find({"pages" : {$eq: 384}})

// db.books.find({"pages" : {$ne: 384}})

// db.books.find({"pages" : {$gt: 384}})

// db.books.find({"pages" : {$gte: 384}})

// db.books.find({"pages" : {$lt: 384}})

// db.books.find({"pages" : {$lte: 384}})

// db.books.find({"pages" : {$in: [384,500,1084]}})

// db.books.find({"pages" : {$nin:[384,500,1084]}})